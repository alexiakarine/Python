real = float(input("Digte o valor que deseja converter: R$ "))
dolar = 5.21

convertido = real / dolar

print(f"O valor R${real} convertido é US${convertido:.2}")

#print("O valor R${} convertido é US${:.2f}".format(real, convertido))
